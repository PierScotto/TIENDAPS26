databases:
  - name: novatech-db
    plan: free
    databaseName: novatech_store
    user: novatech_user

services:
  - type: web
    name: pacto-store-live
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        fromDatabase:
          name: novatech-db
          property: connectionString
      - key: ADMIN_PIER_SCOTTO
        sync: false
      - key: yosoyPONGI2002
        sync: false
